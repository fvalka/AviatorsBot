language: scala

scala:
- 2.12.2

services:
- mongodb
-
before_script:
  - sleep 15 # Used to fix Mongodb issues as recommended by travis https://docs.travis-ci.com/user/database-setup/
script:
- sbt clean coverage test coverageReport

after_success:
- bash <(curl -s https://codecov.io/bash)