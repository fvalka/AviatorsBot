apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aviatorsbot.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "aviatorsbot.name" . }}
    helm.sh/chart: {{ include "aviatorsbot.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "aviatorsbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
